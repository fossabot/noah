// Data definitions for the worker/cache service

syntax = "proto3";

package worker;

message Session {
    bool in_transaction = 1;

    message NodeSession {
        int64 node_id = 1;
        bytes node_session_id = 2;
    }

    repeated NodeSession node_sessions = 2;
}