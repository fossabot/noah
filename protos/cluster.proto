syntax = "proto3";

package cluster;

enum WorkerType {
    Coordinator = 0;
    Tablet = 1;
}

message JoinRequest {
    string region = 1;
    string zone = 2;
    string ip_address = 3;
    uint64 port = 4;
    WorkerType type = 5;
}

message JoinResponse {
    uint64 coordinator_id = 1;

}